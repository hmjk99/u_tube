import React from 'react'
import { logo } from '../utils/constants'

const Nav = () => {
  return (
    <div className='nav'>
        <img id='logo' src={logo} alt='logo'/>
    </div>
  )
}

export default Nav